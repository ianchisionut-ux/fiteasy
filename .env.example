# Baza de date Neon (Postgres)
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# NextAuth — generează un secret cu: openssl rand -base64 32
AUTH_SECRET=""

# Secret pentru /api/setup — folosit o singură dată ca să creezi contul de instructor
SETUP_SECRET=""
